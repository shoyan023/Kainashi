<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width,user-scalable=no,maximum-scale=1" />
  <title>Kainashi</title>
  <link href='http://fonts.googleapis.com/css?family=Nunito:300' rel='stylesheet' type='text/css'>
  <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
  <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header class="header_wrapper" id="headerWrapper">
  <div class="header">

    <a href= "/" class="logo"><img src="/" ></a>

    <div class="menu">
      <a href="#"><span>≡</span>MENU</a>
    </div>
    <div class="drawer">
    <nav>
    <ul class="nav">
      <li class="how_to"><a href="" class="link">HOW TO</a></li>
      <li class="titles"><a href="/theme" class="link">THEMES</a></li>
      <% if user_signed_in? %>
      <li class="login button_login"><a href="<%= user_path(current_user.id) %>" class="button_login"><%= current_user.username %></a>
          <ul class="hidden_list">
            <li><%= link_to 'マイページ', user_path(current_user.id), class: "hidden_link" %></li>
            <li><%= link_to 'プロフィール変更', edit_user_registration_path, class: "hidden_link" %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "hidden_link" %></li>
          </ul>
      </li>
      <% else %>
        <li class="login button_login"><a href="#" class="button_login">SIGN IN / LOGIN</a>
          <ul class="hidden_list">
            <li><%= link_to "サインアップ", new_user_registration_path, class: "hidden_link" %></li>
            <li><%= link_to "ログイン", new_user_session_path, class: "hidden_link" %></li>
          </ul>
        </li>
      <% end %>
    </ul>
    </nav>
    </div>
  </div>
  <script>
    window.addEventListener('load', function() {
    new FastClick(document.body);
    }, false);
    $('.menu').click(function(e){
      $('.nav').slideToggle();
      e.preventDefault();
    });
</script>
</header>

<%= yield %>

</body>
</html>
